package BinarySearchTree;

public class BinaryChecker {
    // you are given the root of a binary tree, write a
    // function to determine if it is a value binary search tree (bst)
    // Root has 2 children,
    // left side must be less then the root
    // right side must be greater than the root

}
